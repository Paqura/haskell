{-# LANGUAGE OverloadedStrings #-}
import qualified Data.Text as T

aWord :: T.Text
aWord = "Work with with"

-- main :: IO()
-- main = do
--   print $ T.splitOn " " aWord
-- ["Work", "with", "with"]

-- что-то типо `join`
-- main :: IO()
-- main = do
--   print $ T.intercalate " " ["Work", "with", "with"]

